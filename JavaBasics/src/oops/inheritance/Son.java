package oops.inheritance;

public class Son extends Father{
    //Son class inherits Father's properties
    //Son --> Father: single inheritance (smart)
    //Son --> Father --> GrandFather: multi-level inheritance (rich)

    //java doesn't support multiple inheritance
}
